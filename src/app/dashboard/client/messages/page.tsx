"use client";

// Member messages page - reuses the same component as creator messages
// The component itself adapts based on the user's role
import MessagesPage from "../../messages/page";

export default function ClientMessagesPage() {
  return <MessagesPage />;
}
